name: Publish

on:
  push:
    branches:
      - version/*
  workflow_dispatch:

jobs:
  build:
    uses: SLNE-Development/surf-workflows/.github/workflows/build-publish-release-gradle.yml@master
    with:
      modules: "surf-stats-paper-*-all.jar;surf-stats-microservice-*-all.jar"
    secrets: inherit
